name: shared_types
description: "Eidolon shared Dart entity/model definitions used across apps"
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^3.0.6
  json_serializable: ^6.9.5
  flutter_test:
    sdk: flutter
